// This component has been removed as requested
// Keeping the interface for type safety in case it's referenced elsewhere
interface ServerTimingSectionProps {
  serverTiming?: string;
}

export default function ServerTimingSection({ serverTiming }: ServerTimingSectionProps) {
  // Component removed as requested
  return null;
}